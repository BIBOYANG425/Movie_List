# C7-iOS Implementation Plan (final parity cycle, iOS half)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Close the last parity gaps: achievements on iOS (surface + the server RPC), the movie-mode in-flow suggestions grid (deferred-sweep #2 — a real web-parity gap), Discover card-action polish (#3), shelf-projection notes on management emissions (#6), and profile deep links — BOTH `spool://u/` and real universal links on rankspool.com (owner config delivered 2026-07-11).

**Architecture:** Achievements granting is SERVER-side (`grant_achievements()` RPC, live in prod — see `docs/contracts/shared-payloads.md ## achievements`): iOS calls it fire-and-forget after rank/journal/follow actions (the contract's RECOMMENDED post-write pattern — an intentional improvement over web's tab-mount-only call) and renders badges from `user_achievements` SELECTs. Clients NEVER write `badge_unlock` notifications (the RPC does — double-write hazard documented). Everything else ports established patterns.

**Tech Stack:** Swift + XCTest. Baselines: iOS 782, web 545 (untouched). No migrations (the RPC is live).

## Global Constraints

- Binding: `docs/plans/audits/2026-07-11-c7-smalls-web-audit.md` (achievements catalog §, deferred-sweep items) + `docs/contracts/shared-payloads.md ## achievements` (the RPC contract — return = newly-granted badge_key text[]; idempotent; no client insert path).
- Descopes hold: NO lists, NO send-3-recs, NO Letterboxd import on iOS.
- L10n: all new copy through `L10n.t` with zh values (the C6 register + voice rules); parity test must stay green.
- Conventional commits + `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`; dated headers bumped; string-extraction discipline where copy moves.

---

### Task 1: AchievementsClient + badge surface

**Files:** Create `ios/Spool/Sources/Spool/Services/AchievementsClient.swift` (actor/enum per repo idiom: `grant() async -> [String]` via `rpc('grant_achievements')` — fire-and-forget wrapper `grantQuietly()` that logs; `badges(for userId:) async throws -> [EarnedBadge]` SELECT from user_achievements; the 16-badge catalog as a pure Swift table mirroring web's `BADGE_CATALOG` [key, name, description, icon, grantable] — cite web lines); Create the badge surface: a section on `ProfileScreen` (own profile) + `FriendProfileScreen` (read-only) rendering earned badges in the app idiom (grid of badge chips; locked badges dimmed on own profile like web's AchievementsView). Tests: catalog parity vs web (literal pin), client decode, grant-return handling.
- Commit `feat(ios): achievements — server RPC client, badge catalog, profile surface`.

### Task 2: Post-write grant hooks

**Files:** Modify `RankPersistence.swift` (after a confirmed save → `AchievementsClient.grantQuietly()`), `JournalDraftModel` save path (after composer save), `FriendsScreen`/follow action (after follow). Fire-and-forget AFTER the primary write succeeds; never gates or delays the primary UX; new badges surface via the existing NotificationBell (the RPC writes badge_unlock rows server-side — verify the bell's type handling renders them; add the type if the bell filters).
- Tests: hook decision seams (grant called only on confirmed success; not on failure/cancel).
- Commit `feat(ios): achievement grants fire after rank, journal, and follow writes`.

### Task 3: Movie-mode in-flow suggestions grid (deferred #2)

**Files:** Modify `RankEntryModel.swift` + `RankEntryScreen.swift` — movie mode gains the suggestions grid exactly like the C5 TV grid (SuggestionsClient .movie, suggestions+backfill prefetch, consume-splice, refill <3 at page+1, Refresh both modes, session excludes cap 200; the tv-grid code generalizes — parameterize rather than duplicate). Signed-out movie mode keeps its demo-fixtures posture (adjudicate against the existing gates — do NOT regress the fixtures path).
- Tests: extend the tv-grid choreography tests to cover the movie path (parameterized).
- Commit `feat(ios): movie suggestions grid in the rank flow (web-parity gap closed)`.

### Task 4: Discover card actions polish (#3) + shelf-notes emissions (#6)

**Files:** Discover: add the live `isAlreadyOwned`-style display filter for same-session ranked/saved items (web parity — read web DiscoverView's render-time filter) + the rank-flow per-suggestion save-for-later affordance if the seam is cheap (adjudicate: the rank-entry grid is onPick-only; a long-press or small bookmark affordance — implementer judgment, note the decision). Shelf emissions: `RankedItem` projection gains `notes` (read `getAllRankedItems`' select — add the column) so `RankMoveEmitter`/`RankRemoveEmitter` metadata carries `{notes?, year?}` like web (closes the C4-UI analytics divergence).
- Tests: projection decode + emitter payload with notes; filter behavior.
- Commit `feat(ios): Discover owned-filter + save affordance; management emissions carry notes`.

### Task 5: Profile deep links — spool://u/ AND real universal links (owner config received 2026-07-11)

**Owner-provided config (verbatim, binding):** domain `rankspool.com`, Team ID `J5FN3M53L4`, Bundle ID `com.yangb.spool`. Remaining owner step: enable the Associated Domains capability on the App ID (developer.apple.com → Identifiers → com.yangb.spool) — Xcode automatic signing can also add it on next archive; note in the PR body.

**Files:**
1. `SpoolAppRoot.swift` — `onOpenURL` handling BOTH `spool://u/{username}` and `https://rankspool.com/u/{username}` (SwiftUI delivers universal links through onOpenURL on iOS 16): parse → resolve username → present the friend profile; unknown/failed → toast; the `spool://auth-callback` OAuth path untouched. Pure URL-parsing seam tested (u-path extraction from both schemes, www variant, callback passthrough, garbage rejection).
2. `ios/SpoolApp/` — create `SpoolApp.entitlements` with `com.apple.developer.associated-domains` = [`applinks:rankspool.com`, `applinks:www.rankspool.com`] and wire `CODE_SIGN_ENTITLEMENTS` into the SpoolApp target's build configurations (pbxproj edit — the SpoolSimulator harness does NOT need it).
3. `public/.well-known/apple-app-site-association` (web static asset, extension-less) — `{"applinks":{"apps":[],"details":[{"appIDs":["J5FN3M53L4.com.yangb.spool"],"components":[{"/":"/u/*","comment":"public profiles"}]}]}}` — plus a `vercel.json` headers rule forcing `content-type: application/json` for that exact path (Vercel serves extension-less static files as octet-stream/text otherwise; Apple's CDN requires JSON). Verify the existing vercel.json rewrite rule doesn't swallow `/.well-known/*` (SPA rewrites usually exclude files that exist — check; add an explicit exclusion if needed).
- Commit `feat: universal links — AASA on rankspool.com, entitlements, https+spool u-path routing`.

### Task 6: Docs + ledger

**Files:** `docs/plans/2026-07-07-ios-parity-ledger.md`: C7 COMPLETE both halves → **PARITY PROGRAM COMPLETE** (all cycles C0-C7 + mini-cycles shipped; DoD check: every live web feature has an iOS equivalent [descopes ledgered], both clients write identical shapes, TMDB key absent from both bundles); the remaining-items list (design-check cycle next [owner screenshot seed]; owner infra: Apple UL, Photon agent; deferred backlog pointer). `docs/contracts/shared-payloads.md` only if a shape changed (emission notes addition — update the management-emission note).
- Commit `docs: C7 complete — parity program done`.

## Self-Review Notes
- Task 2's bell verification matters: the RPC writes badge_unlock rows now; if the iOS bell type-filters, badges silently vanish — check NotificationRepository/bell rendering.
- Task 3 must parameterize the tv grid, not fork it (the C5 review would flag duplication).
- Contract-doc edits in T6 get an extra fact-check pass (the ledgered pattern: docs drift is what final reviews keep catching).
